Show the current Opnble dev stack state.

Run `make status` and present the output as-is. Then in one short paragraph: which components are running, which are stopped, and the single most useful next command for the current state.
